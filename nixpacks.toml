[phases.setup]
nixPkgs = ["nodejs_20", "python313", "gcc"]

[phases.install]
cmds = [
  "npm install",
  "python3 -m venv .venv",
  ".venv/bin/pip install browser-use-sdk",
]

[start]
cmd = "node server.js"
